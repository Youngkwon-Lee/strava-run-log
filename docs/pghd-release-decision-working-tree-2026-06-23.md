# PGHD Release Decision: Working Tree

Date: 2026-06-23 KST

## Decision

Result: `ready_for_release_review`

Candidate: `working-tree`

Environment: `local`

Apply path: `physio-app-owner-lineage`

Decision artifact:
`output/pghd-release-decision/latest.json`

## Commands Run

The decision was generated by:

```bash
PGHD_RELEASE_CANDIDATE=working-tree \
PGHD_RELEASE_ENVIRONMENT=local \
npm run gate:pghd:decision
```

The sequential gate recorded these successful commands:

- `npm run gate:pghd:release`
- `npm run gate:pghd:physio-release`
- `npm run gate:pghd:strict-staging`
- `npm run check:pghd:status`

## Evidence Summary

- `failedCommand: null`
- `decision.ok: true`
- `decision.evidence.readinessOk: true`
- `decision.evidence.ownerBridgeApplied: true`
- `decision.evidence.missingGateEvidence: []`
- `decision.evidence.blockers: []`
- `decision.evidence.dbPushAllowed: false`
- `decision.evidence.dbPushBlocked: true`
- `decision.evidence.localMigrationHistoryOk: false`

## Gate Highlights

- Unit and API suite passed with 149 tests.
- Dashboard viewport smoke passed for desktop and mobile screenshots.
- Normal PGHD smoke passed with warning-level preflight for the reused fallback
  subject.
- Strict staging smoke passed with temporary org-client context, persisted Human
  State materialization, and `preflightStatus: "ok"`.
- PhysioApp handoff static surface passed.
- Smoke cleanup reported zero leftover bootstrap connections, smoke runs, and
  active smoke persons.
- `check:pghd:status` passed with `recommendedApplyPath:
  "physio-app-owner-lineage"`.

## Residual Risks

- Broad `supabase db push` from `strava-run-log` remains blocked by design.
- Linked Supabase migration history is still owned by the PhysioApp lineage.
- `localMigrationHistoryOk: false` is expected and acceptable only because
  `ownerBridgeApplied: true` is proven.

## Next Action

Attach the full terminal output or the generated decision artifact when
reviewing the release. Keep using the PhysioApp owner-lineage path for schema
ownership; do not apply broad run-log migrations directly to the linked project.
